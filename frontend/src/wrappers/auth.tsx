import { ReactNode } from "react";
import { Button } from 'antd';

interface IProps {
  children?: ReactNode
}

const Index = (props: IProps) => {
  return (
    <div>
      123
    </div>
  );
}

export default Index;
